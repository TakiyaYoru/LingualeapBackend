# ===========================================
# LINGUALEAP BACKEND ENVIRONMENT VARIABLES
# ===========================================

# Server Configuration
PORT=4001
NODE_ENV=development

# MongoDB Atlas Configuration
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/lingualeap?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration (cần cho authentication)
JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_secure
JWT_EXPIRES_IN=7d

# ===========================================
# SETUP INSTRUCTIONS:
# ===========================================
# 1. Copy this file to .env
# 2. Replace <username> and <password> với MongoDB Atlas credentials
# 3. Replace xxxxx với MongoDB cluster URL
# 4. Tạo JWT_SECRET mạnh và an toàn (có thể dùng: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
# 5. Restart server sau khi update .env